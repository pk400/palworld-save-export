# palworld-save-export

# How to build

1. Start virtual env and install packages

```
pip install -r requirements.txt
```

2. Build executable

```
pyinstaller main.py --name=palworld-server-export
```
